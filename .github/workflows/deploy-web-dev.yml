name: Deploy website (DEV)

on:
  push:
    branches: [ develop ]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Skapa config.json från environment variable 'API_BASE'
      - name: Create config.json
        run: |
          if [ -z "${{ vars.API_BASE }}" ]; then
            echo "❌ Missing vars.API_BASE in environment 'dev'"; exit 1;
          fi
          echo "{\"apiBase\":\"${{ vars.API_BASE }}\"}" > ./config.json
          echo "Wrote config.json:"
          cat ./config.json

      - name: Deploy to Azure Static Web Apps (DEV)
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_DEV }}
          action: upload
          app_location: "/"          # dina statiska filer i repo-roten
          output_location: "/"       # ingen separat build-mapp
          app_build_command: ""      # <- kör inget build-steg
          api_build_command: ""      # <- inget API-build heller
